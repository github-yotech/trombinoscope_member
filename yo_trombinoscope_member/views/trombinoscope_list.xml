<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="trombinoscope_list_list" model="ir.ui.view">
        <field name="name">trombinoscope_list.list</field>
        <field name="model">trombinoscope.list</field>
        <field name="arch" type="xml">
            <list string="Trombinoscope">
                <field name="name"/>
                <field name="is_active" />
            </list>
        </field>
    </record>

    <record id="trombinoscope_list_form" model="ir.ui.view">
        <field name="name">trombinoscope_list.form</field>
        <field name="model">trombinoscope.list</field>
        <field name="arch" type="xml">
            <form string="Trombinoscope">
                <sheet>
                    <group>
                        <group>
                            <field name="name"/>
                            <field name="is_active" />
                        </group>
                        <group>
                            <field name="member_placeholder_image" widget="image"/>
                        </group>
                    </group>
                    <group>
                        <field name="member_ids"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="trombinoscope_list_member_list" model="ir.ui.view">
        <field name="name">trombinoscope.list.member.list</field>
        <field name="model">trombinoscope.list.member</field>
        <field name="arch" type="xml">
            <list editable="bottom" create="1" delete="1">
                <field name="sequence" widget="handle"/>
                <field name="partner_id" domain="[('membership_state', 'in', ['invoiced', 'paid', 'free'])]"/>
            </list>
        </field>
    </record>

    <record id="action_trombinoscope_list" model="ir.actions.act_window">
        <field name="name">Trombinoscope</field>
        <field name="res_model">trombinoscope.list</field>
        <field name="view_mode">list,form</field>
    </record>

    <menuitem
        id="trombinoscope_list_menu"
        name="Trombinoscope"
        action="action_trombinoscope_list"
        parent="website.menu_website_global_configuration"
        sequence="70"/>

</odoo>
